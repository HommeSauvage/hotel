{
  "name": "hotel-root",
  "private": true,
  "workspaces": [
    "api",
    "app",
    "lib/*"
  ],
  "scripts": {
    "dev": "now dev --listen 8000",
    "seed": "ts-node internals/seed/index.ts"
  },
  "devDependencies": {
    "@types/faker": "^4.1.5",
    "dotenv": "^8.1.0",
    "faker": "^4.1.0"
  }
}
